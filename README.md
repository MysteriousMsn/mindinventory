# Dummy data will be added after hitting register end point.

# notification scheduling function written in cron folder which is executed in index.js

# we can also use node-corn wrapper on top of node schedule to run by itself on a specific time.